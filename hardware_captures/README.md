# Hardware Captures

Use this folder for local-only BIOS, firmware, and runtime hardware captures.

Files in this folder are ignored by Git because they may contain:
- MAC addresses
- system UUIDs
- license IDs
- disk serials
- network information
- other machine-specific identifiers

Keep sanitized summaries in `docs/` when the information is useful for the project.
