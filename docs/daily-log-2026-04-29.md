# Daily Log - 2026-04-29

Single running handoff for today's work. Add a new entry after each meaningful change so VS Code or terminal crashes do not lose context.

## Current State

- Machine target: `192.168.0.1`, Modbus TCP port `502`, unit id `1`.
- Active logger run pointer: `data/live_outputs/active_run.txt`.
- Current dashboard URL: `http://127.0.0.1:8000`.
- Current active run at last update: `data/live_outputs/20260429/110523`.
- Logger starts and writes files correctly, but the machine is currently closing Modbus reads with `10054`, so live logs show reconnects and `ok_values=0`.

## Working Rule

- After every code/tooling/config change, append a short entry here with:
  - Time
  - Files changed
  - What changed
  - Verification
  - Next pickup notes

## Entries

### 2026-04-29 11:05 - Logger routing fixed

Files changed:
- `scripts/start_live_output_log.ps1`
- `scripts/add_operator_marker.ps1`
- `tools/start_node_live_logger.py`
- `tools/node_start_edge_ui.py`
- `tools/node_patch_fault_marker.py`
- `tools/node_patch_dashboard_logger_status.py`
- `tools/node_patch_hmi_login_ui.py`

What changed:
- Fixed live logger startup so Windows paths with spaces do not break the `--output-dir` argument.
- Live logger now writes `data/live_outputs/active_run.txt`.
- Marker script now writes to the active run instead of guessing latest folder.
- Start script stops previous live loggers by default before starting a new run.
- Node/helper scripts now target this Desktop repo instead of the missing `$HOME/AI-Fault-Detection` path.

Verification:
- Started logger with `scripts/start_live_output_log.ps1 -MachineIp 192.168.0.1 -PollSeconds 2`.
- Active run created under `data/live_outputs/20260429/110523`.
- Marker script wrote successfully into the active run.
- One-register Modbus probe failed with connection closed by remote host, confirming the no-data issue is machine/endpoint behavior rather than the log file path.

Next pickup:
- If Modbus values remain at `ok_values=0`, check the machine Modbus endpoint/session state before tuning code.

### 2026-04-29 11:55 - Persistent dashboard added

Files changed:
- `edge_monitor/__init__.py`
- `edge_monitor/server.py`
- `tools/node_patch_persistent_dashboard_state.py`

What changed:
- Added a lightweight built-in HTTP dashboard server using Python standard library.
- Dashboard restores previous log tail and latest HMI screenshot after restart.
- Added API endpoints:
  - `/api/persistent-state`
  - `/api/current`
  - `/api/hmi/latest-snapshot`
  - `/api/markers/fault`
- Added patch helper for future dashboard persistence changes.

Verification:
- `python -m py_compile edge_monitor/server.py` passed.
- Dashboard launched through `tools/node_start_edge_ui.py`.
- `http://127.0.0.1:8000/api/persistent-state` returned current run and snapshot metadata.

Next pickup:
- Keep HMI screenshots in `data/vnc_screenshots`; the dashboard uses the newest `.png` unless `latest_snapshot.txt` points to a specific file.

### 2026-04-29 12:08 - Dashboard tabs reorganized

Files changed:
- `edge_monitor/server.py`

What changed:
- Converted dashboard to one page with four tabs:
  - `Restored Log`
  - `HMI Picture`
  - `Live Log`
  - `Live Changes`
- Added `/api/live-state` for current run tail, recent `changes.csv`, and recent markers.
- Live tabs refresh every 3 seconds while active.

Verification:
- `python -m py_compile edge_monitor/server.py` passed.
- Restarted dashboard through `tools/node_start_edge_ui.py`.
- `http://127.0.0.1:8000/` returned status `200`.
- Page contained all four tab labels.
- `http://127.0.0.1:8000/api/live-state` returned current run data.

Next pickup:
- If the dashboard looks stale after a browser crash, reload `http://127.0.0.1:8000`; it should restore from disk.

### 2026-04-29 12:23 - Restored tab now uses last informative run

Files changed:
- `edge_monitor/server.py`
- `docs/daily-log-2026-04-29.md`

What changed:
- The dashboard `Restored Log` view now prefers the newest run with successful Modbus reads (`ok_values > 0`) instead of blindly using `active_run.txt`.
- The `Live Log` and `Live Changes` tabs still use the current active run.
- The restored run label now shows both restored and active run paths when they differ.

Verification:
- `python -m py_compile edge_monitor/server.py` passed.
- Restarted dashboard through `tools/node_start_edge_ui.py`.
- `/api/persistent-state` now points restored data at `data/live_outputs/20260429/092625`.
- `/api/live-state` still points live data at `data/live_outputs/20260429/110523`.

Next pickup:
- Detailed morning data is still in `20260429/092625`; current live run `110523` has reconnect/no-data lines because Modbus reads are being closed by the machine endpoint.

### 2026-04-29 12:32 - Machine D: export copied and parsed

Files changed:
- `data/machine_exports/20260429_122132/BP_ALVStoricAllarms.htm`
- `data/machine_exports/20260429_122132/BP_ALVStoricState.htm`
- `data/machine_exports/20260429_122132/BP_ALVStoricData.htm`
- `data/machine_exports/20260429_122132/alarms.csv`
- `data/machine_exports/20260429_122132/states.csv`
- `data/machine_exports/20260429_122132/data_changes.csv`
- `data/machine_exports/20260429_122132/summary.md`
- `docs/daily-log-2026-04-29.md`

What changed:
- Copied three machine HTML exports from `D:\` into `data/machine_exports/20260429_122132`.
- Parsed the exports into CSV files for alarms, machine states, and data/format changes.
- Created `summary.md` with counts and recent fault/state/change highlights.

Verification:
- Parsed counts:
  - 48,885 alarm/event records
  - 36,228 machine state records
  - 1,010 data/format change records
- Recent 2026-04-29 alarms include repeated `BAR SAFETY`, `FILM FINISHED`, `PRODUCT FLOW END`, plus `B12 OBSTRUCTION`, `PRESS POWER`, and `EMERGENCY DOOR OPEN 1`.
- Data changes around the current issue are mostly 2026-04-28 `REEL_UP_BELT` acceleration/deceleration/frequency tuning. No 2026-04-29 parameter changes appeared before the export.

Next pickup:
- Use `data/machine_exports/20260429_122132/summary.md` first for quick review.
- Cross-check `BAR SAFETY` timestamps around `10:10`, `10:13`, `10:21`, `10:23`, `10:27-10:28`, `10:31`, `10:36`, and `12:08` against physical seal-bar/B12 safety behavior.

### 2026-04-29 12:40 - Focus narrowed to BAR SAFETY

Files changed:
- `data/machine_exports/20260429_122132/summary.md`
- `docs/daily-log-2026-04-29.md`

What changed:
- Updated the machine export summary to treat `PRODUCT FLOW END` as expected low-product/waiting behavior.
- Marked `FILM FINISHED` as noisy context, not the main root-cause target.
- Promoted `BAR SAFETY` as the primary concern and listed the 2026-04-29 active/resolved timestamps.

Verification:
- Re-read parsed `alarms.csv` for 2026-04-29 `BAR SAFETY`.
- Re-read parsed `states.csv`; BAR SAFETY events align with repeated `SYSTEM IN STOP` transitions.

Next pickup:
- Focus troubleshooting on the bar safety chain: physical bar safety device, seal-bar/B12-related safety interlocks, wiring/connector movement during motion, and any brief safety-channel drops.

### 2026-04-29 12:50 - Correlated BAR SAFETY to Modbus candidates

Files changed:
- `data/machine_exports/20260429_122132/bar_safety_modbus_correlation.csv`
- `data/machine_exports/20260429_122132/bar_safety_modbus_correlation.md`
- `docs/daily-log-2026-04-29.md`

What changed:
- Built a focused correlation report comparing HMI `BAR SAFETY` ACTIVE timestamps against decoded Modbus signal changes.
- Checked the good Modbus run from `data/live_outputs/20260429/092625` against HMI alarm export `data/machine_exports/20260429_122132/alarms.csv`.
- Focused the comparison on B2 candidates, B12 candidates, seal-bar/B12 secondary candidates, running state, and machine stopped state.

Verification:
- For the 11 current-morning BAR SAFETY events inside the good Modbus run, all 11 had B2 movement within +/-30 seconds.
- All 11 had seal/B12 secondary movement within +/-30 seconds, usually with the nearest focused Modbus change within about half a second of the HMI alarm timestamp.
- 10 of 11 had direct B12 candidate movement within +/-30 seconds.
- The strongest current Modbus correlation is `seal_bar_position_a` / `seal_bar_position_b` at addresses `10325` / `13539`, especially transitions involving `seal_bar_up_or_B12_tripped_secondary`.
- The 12:08 BAR SAFETY events were outside the good Modbus run window, so they still need to be caught with the fresh logger.

Next pickup:
- Treat Modbus B2/B12/seal-bar values as correlating candidates, not a proven alarm bit yet.
- Use the fresh logger plus the verified fault button to catch the next BAR SAFETY event and confirm the same pattern live.

### 2026-04-29 12:55 - B1/B2 sensors replaced

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- Recorded that B1/B2 sensors have been replaced.
- B1/B2 use SICK sensor PN `1040752`, so the sensor bodies themselves should not be treated as the primary suspect unless new evidence points back there.
- Added context that the replaced sensors were suspected of being exposed to excessive temperature and effectively failing from heat.

Current interpretation:
- Keep focusing BAR SAFETY troubleshooting on alignment, bracket/mechanical trip position, wiring/connector movement, input timing, heat exposure around the sensor mounting area, and the safety/PLC interpretation of the B1/B2/B12 chain rather than assuming newly installed B1/B2 sensor bodies are failed.
- OEM position: current B1/B2 sensor placement is acceptable and the sensors should tolerate the location.
- In-house concern: heat exposure at the B1/B2 mounting area may still be contributing to failures or unstable behavior, despite OEM guidance.
- Treat heat exposure as an open hypothesis to prove or disprove with fault timing, physical temperature checks, and any visible heat damage/discoloration on sensors, wiring, or connectors.

### 2026-04-29 13:00 - Next normal-operation checks for BAR SAFETY

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- Added next practical troubleshooting targets for getting the machine back to normal operation without bypassing safety.

Recommended checks:
- Verify B1/B2/B12 alignment dynamically during a running cycle, not only while stopped.
- Watch for bracket flex, vibration, or thermal movement that changes the sensing point after warmup.
- Check B1/B2/B12 cables and connectors for heat damage, loose pins, intermittent movement, or strain during bar motion.
- Confirm actual sensor supply voltage and input voltage at the PLC/safety input during the fault, if accessible.
- Capture the next BAR SAFETY fault with the live logger and fault button, then compare the exact B2/B12/seal-bar transition order.
- If the fault happens after warmup, record sensor/bracket/cable temperatures and compare cold vs hot behavior.
- Do not bypass or defeat the safety chain; any test should preserve the safety function.

### 2026-04-29 13:05 - Three back-to-back failures reported

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- Recorded operator report of three failures back-to-back.
- Checked active logger run `data/live_outputs/20260429/123824` and restarted a fresh run at `data/live_outputs/20260429/130017`.
- Verified TCP connectivity from this PC to machine `192.168.0.1:502`.

Verification:
- Active logger before restart had `ok_values=0`; no successful Modbus reads were captured during the reported failures.
- `operator_markers.csv` only contained the earlier `FAULT_BUTTON_TEST`; no marker was recorded for the three-failure cluster.
- After restart, single-register Modbus probes to the known B2/B12/seal-bar addresses still failed immediately; the machine accepted TCP connection but forcibly closed Modbus reads.
- Current PC source IP was observed as `192.168.0.30`, while earlier working notes expected the node/PC at `192.168.0.20`.

Next pickup:
- The machine does not expect or require this diagnostic logger/node; the node is an added diagnostic tool.
- Do not assume `192.168.0.20` is required by the machine unless new evidence proves it.
- Investigate Modbus read failures as an endpoint/session/read-pattern issue: possible existing HMI/PLC client limit, busy endpoint, unsupported register/function request, or diagnostic node network configuration problem.
- Once Modbus reads succeed again, capture another BAR SAFETY fault and compare B2/B12/seal-bar transition order.
- If another fault happens before Modbus is restored, record exact wall-clock times manually so they can be matched to the HMI export later.

### 2026-04-29 13:10 - Clarified diagnostic node role

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- Corrected earlier interpretation that the machine may expect the diagnostic node at a specific IP.
- User clarified the machine is not expecting any logger; the node is an added diagnostic device.

Current interpretation:
- Modbus read failures should be treated as a logger/session/read-pattern/network issue, not as a known machine allowlist expectation.
- Keep the network IP observation as context only: current PC was `192.168.0.30`; earlier notes mentioned node `192.168.0.20`.

### 2026-04-29 13:15 - Enforced single Modbus logger client

Files changed:
- `app/live_output_logger.py`
- `docs/daily-log-2026-04-29.md`

What changed:
- User clarified there should only be one client connected to the machine: the diagnostic node/logger.
- Added a single-instance lock to `app/live_output_logger.py`.
- If a second live logger is started, it exits before opening Modbus so only one logger can connect to `192.168.0.1:502`.
- Confirmed the dashboard should only read local files and not connect to the machine.

Verification:
- Ran `python -m py_compile app/live_output_logger.py`.
- Checked TCP connections and found no active connections to `192.168.0.1:502` after stopping the failing logger.
- Dashboard process is running; live Modbus logger is stopped until the next deliberate capture attempt.

### 2026-04-29 13:15 - Paused node-side logging and tested laptop capture

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- Stopped all local logger/dashboard Python processes to clear the node-style setup.
- Confirmed no active TCP connections to `192.168.0.1:502`.
- Started a fresh laptop logger run at `data/live_outputs/20260429/131231`.
- Restarted the WebUI/dashboard at `http://127.0.0.1:8000`.
- Tested the WebUI fault-marker endpoint.

Verification:
- The laptop logger connected TCP but every Modbus read returned `ok_values=0`; the machine closed the connection on reads.
- Retested exact known-good holding register reads from the earlier good run, including address `10050`; reads were still closed/reset immediately.
- Stopped the failing Modbus logger so it would not keep reconnecting.
- WebUI marker endpoint succeeded and wrote:
  - `2026-04-29T13:14:48.716-05:00,FAULT_BUTTON_LAPTOP_TEST,WebUI marker endpoint test after pausing node logger`

Current status:
- Dashboard is running.
- Modbus logger is stopped because this laptop cannot currently get register values from `192.168.0.1:502`.
- Fault button/marker path works and writes to the active run marker file.

Next pickup:
- Before running product for data capture, restore a working Modbus read path or accept that only manual/WebUI fault markers and HMI export times will be available.
- If product is run now, use the WebUI fault button for exact operator timing and export HMI alarms afterward for the actual machine fault record.

### 2026-04-29 13:20 - Direct laptop connection and machine power-cycle prep

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- User connected laptop directly to the machine, removing the switch and diagnostic node from the path.
- Started direct-connect logger run `data/live_outputs/20260429/131602`.
- User decided to power cycle the machine.

Verification:
- Laptop direct IP path was `192.168.0.30 -> 192.168.0.1:502`; TCP port test succeeded.
- Direct-connect logger still showed `ok_values=0`; Modbus TCP connected, but reads were closed/reset by the machine.
- Stopped the live logger before machine power cycle so no logger remains actively reconnecting during shutdown/startup.

Next pickup:
- After machine power cycle completes, start exactly one fresh logger run.
- Confirm `ok_values > 0` before product testing.
- If reads still fail after power cycle, test with a minimal single-register read before running the full broad logger.

### 2026-04-29 13:20 - Modbus logging restored after power cycle

Files changed:
- `docs/daily-log-2026-04-29.md`

What changed:
- Machine came back up after power cycle.
- Confirmed direct laptop connection to machine at `192.168.0.1:502`.
- Started fresh logger run `data/live_outputs/20260429/131837`.
- Verified WebUI fault-marker path against the fresh active run.

Verification:
- Minimal Modbus reads succeeded after power cycle:
  - Holding `10050:1` returned a value.
  - Holding `10050:50` returned a block.
  - Holding `10325:1` returned a seal/B12 secondary value.
- Full logger run `131837` is healthy with `ok_values=1300` per cycle.
- `changes.csv` is actively receiving register changes.
- WebUI marker endpoint wrote:
  - `2026-04-29T13:19:28.538-05:00,FAULT_BUTTON_POST_POWER_CYCLE_TEST,Marker endpoint test after power cycle and healthy Modbus logging`

Current status:
- Direct laptop Modbus logging is active and healthy.
- Dashboard/WebUI is running at `http://127.0.0.1:8000`.
- Use the WebUI fault button during product run if BAR SAFETY occurs.

### 2026-04-29 13:25 - Three bad packs reported during healthy logging

Files changed:
- `data/live_outputs/20260429/131837/bad_packs_132457_summary.md`
- `docs/daily-log-2026-04-29.md`

What changed:
- User reported three more bad packs while run `data/live_outputs/20260429/131837` was actively logging.
- Added marker `BAD_PACKS_3_REPORTED` after the report.
- Decoded the active run and summarized focused B2/B12/seal-bar/machine-state changes around the report.

Verification:
- Logger remained healthy at `ok_values=1300`.
- Marker file contains:
  - `2026-04-29T13:24:57.475-05:00,BAD_PACKS_3_REPORTED,Operator reported 3 bad packs; marker added after report`
- In the 90 seconds before the report marker, there were many B2/seal-bar/B12-related movements but no `machine_state` change and no `running_enable` change.
- Earlier stop/running transitions were seen around `13:21:50`, `13:22:01`, and `13:22:10`; those need operator/HMI context to know if they correspond to the actual bad packs.

Current interpretation:
- This looks different from a clean BAR SAFETY stop capture.
- The current evidence points more toward pack-quality/timing behavior while running, unless the actual bad-pack moments were closer to the earlier stop/running transitions.

Next pickup:
- For the next bad packs, press the WebUI fault button once per bad pack if practical so each pack has its own narrow timestamp.
- If the issue is bad pack quality rather than a machine stop, review seal-bar timing, product/film presentation, and B2/B12 transitions around each marker.

### 2026-04-29 13:30 - Reviewed earlier stop times and system access options

Files changed:
- `data/live_outputs/20260429/131837/stop_sequence_132135_132220_summary.md`
- `docs/daily-log-2026-04-29.md`

What changed:
- Reviewed the earlier stop/running transitions around `13:21:35-13:22:20`.
- Created a focused stop-sequence summary for run `data/live_outputs/20260429/131837`.
- Checked common TCP services on `192.168.0.1`.

Verification:
- Strong stop moments found at:
  - `13:21:50.880` machine_state `running -> stopped`
  - `13:22:01.719` machine_state `running -> stopped`
  - `13:22:10.409` machine_state `running -> stopped`
- Those stop moments line up with B2 movement and seal/B12 secondary movement in the same 1-2 second neighborhood.
- `event_process_value` briefly decoded as `estop_or_safety_drop` at `13:21:56.308`.
- Network check found Modbus TCP `502` open and SSH `22` open on `192.168.0.1`.

Current interpretation:
- The earlier `13:21:50`, `13:22:01`, and `13:22:10` windows look more like the BAR SAFETY/stop pattern than the later broad bad-pack marker.
- Further system access may be possible through SSH, but only with OEM/authorized credentials and approval; do not attempt bypass.

Next pickup:
- Compare these stop times to the HMI alarm export after the run.
- Ask OEM whether SSH access is supported and what logs/register maps are safe to read.
- Continue using Modbus logging as the primary non-invasive data source unless OEM grants deeper access.
