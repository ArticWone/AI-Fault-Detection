# API Integration Note

This BookStack instance is connected to the SMI node API workflow.

- Base URL: `http://192.168.0.20:6875`
- API token is stored locally on the node at `/srv/smi-ai/config/bookstack-api.env`.
- Do not paste the token into documentation pages.
- Use API updates to keep machine documentation current from logs, snapshots, and repo notes.
